formulas(assumptions).

    all a all b (
        neither_is_understood_through_the_other(a, b)
        <->
        (
            -can_be_understood_through(a, b)
            & -can_be_understood_through(b, a)
        )
    ).

end_of_list.

% vim:ft=prover9
