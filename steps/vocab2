formulas(assumptions).

    all a all b (
        can_be_the_cause_of(a, b)
        <->
        (
            -can_be_the_cause_of(a, b)
            & -can_be_the_cause_of(b, a)
        )
    ).

end_of_list.

% vim:ft=prover9
